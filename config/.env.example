# LinkedIn Job & Internship Bot - Professional Configuration
# Copy this file to .env and configure your settings

# ================================
# REQUIRED CONFIGURATION
# ================================

# Your Telegram Bot Token (Get from @BotFather on Telegram)
TELEGRAM_TOKEN=your_telegram_bot_token_here

# ================================
# SEARCH CONFIGURATION (Optional)
# ================================

# Default search location (India-focused strategy)
DEFAULT_LOCATION=India

# Maximum number of results to return per search (1-50)
MAX_RESULTS=10

# Progressive time search filters (LinkedIn time filter codes)
# The bot will try these timeframes in order until jobs are found:
# TIME_FILTER_1DAY=r86400    # Last 24 hours (primary search)
# TIME_FILTER_2DAY=r172800   # Last 2 days (fallback 1)
# TIME_FILTER_7DAY=r604800   # Last 7 days (fallback 2)

# ================================
# WEBDRIVER CONFIGURATION (Optional)
# ================================

# Path to ChromeDriver executable (auto-detected if not specified)
# CHROME_DRIVER_PATH=/path/to/chromedriver

# WebDriver timeouts in seconds
PAGE_LOAD_TIMEOUT=30
IMPLICIT_WAIT=10

# ================================
# LOGGING CONFIGURATION (Optional)
# ================================

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log file path (leave empty for console only)
LOG_FILE=linkedin_bot.log
